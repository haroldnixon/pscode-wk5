// Exercise 1: Calculate the Sum of Two Numbers

a + b = c

num1 + num2 = sum
print sum

ask for 2 numbers 
add the 2 number together 
print on the screen the sum of those 2 numbers


// Exercise 2: Check if a Number is Even or Odd

ask for x
if { x % 2 }
	print('Even')
else
	print('Odd')

// Exercise 3: 
Write pseudo code that will perform the following.    

a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

#A - Read in Numbers
Ask for 5 numbers and store them in varablies numA - numE.

#B - Average
numA + numB + numC + numD + numE = numTotal
numAverage = numTotal / 5

#C - Find the Smallest & Largest
Add to array i
Iterat i [numA, numB, numC, numD, numE]
	numMax = 0
	if  {i > numMax
		numMax = i
		}
	numMin = numMax
	if {
		i < numMax
		numMin = i
		}
#D - Results
print(numAverage ' is the average of the 5 numbers entered a moment ago.')
print(numMax ' is the largest of the numbers while ' numMin ' is the lowest.') 

-- END --
